#ifndef _MY_STRING_
#define _MY_STRING_
int my_strlen(char*);
int word_count(char*);
int upper_count(char*);
int isupper(char);
char toupper(char);
char* capitalize(char*);
char* my_strflip(char*);
#endif